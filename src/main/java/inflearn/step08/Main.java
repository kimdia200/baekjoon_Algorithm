package inflearn.step08;

public class Main {
    public static void main(String[] args) {
        int[] arr = {1,2,3,4,5};
        dfs(arr,0);
    }

    private static void dfs(int[] arr, int idx) {
        if(idx==arr.length)
    }
}
